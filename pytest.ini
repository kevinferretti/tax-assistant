[pytest]
# Only the deterministic unit tests run by default / in CI. The opt-in evals
# under evals/ are launched explicitly (python -m evals.run_evals) and never
# auto-run, so they don't gate CI or cost API calls on every push.
testpaths = tests
